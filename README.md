# workbench_setup

``` bash
# Choose distro
cd <ubuntu_xx_yy>

# Run workbench setup script
bash setup_workbench.sh

# Install ROS
bash install_ros.sh

# Initialize the catkin workspace
cd ~/catkin_ws && catkin_make
```
